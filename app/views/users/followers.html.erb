<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <div class="icon-name">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
      </div>
      <%= render partial:"profile_text", locals:{user:@user} %>
      <div class="num_info row">
      <%= render partial: 'num_info' ,　locals: {user: @user} %>
      </div>
    </section>
  </aside>
  <div class="col-md-8">
    <% if @follower_users.any? %>
      <h3>Follower (<%= @follower_users.count %>)</h3>
      <ol class="follower">
        <%= render partial: 'follower_user', collection: @follower_users %>
      </ol>
    <% end %>
  </div>
</div>