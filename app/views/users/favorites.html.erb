<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <div class="icon-name">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
      </div>
      <%= render partial:"profile_text", locals:{user:@user} %>
      <div class="num_info row">
      <%= render partial: 'num_info' ,　locals: {user: @user} %>
      </div>
    </section>
  </aside>
  <div class="col-md-8">
    <% if @favorite_microposts.any? %>
      <h3>Favorite (<%= @favorite_microposts.count %>)</h3>
      <ol class="favorite_microposts">
        <%= render @favorite_microposts %>
      </ol>
    <% end %>
  </div>
</div>